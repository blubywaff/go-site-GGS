<!DOCTYPE html>
<html lang="en">
{{template "globalhead" "Galleta Games and Software"}}

<style>
    * {
        cursor: default;
    }

    #navbar {
        --background: #ff2020;
        --text-color: #ffffff;
    }

    #navbar {
        background-color: var(--background);
        padding: 20px;
        min-height: 32px;
    }

    #navbar .element {
        display: inline-block;
        align-content: baseline;
    }

    #account {
        float: right;
    }

    #navbar .element span {
        font-size: x-large;
        margin-right: 20px;
        color: var(--text-color);
        text-decoration: none;
        cursor: pointer;
    }

    #navbar .element #sidenav-button {
        cursor: pointer;
        display: inline-block;
        margin-right: 20px;
    }

    #navbar .element #sidenav-button:hover * {
        background-color: #cccccc;
    }

    #navbar .element #sidenav-button * {
        width: 20px;
        height: 3px;
        background-color: var(--text-color);
        margin: 4px 0;
        transition: 0.5s;
        cursor: inherit;
    }

    .change #bar1 {
        transform: translate(0px, 7px) rotate(-45deg);
    }

    .change #bar2 {
        opacity: 0;
    }

    .change #bar3 {
        transform: translate(0px, -7px) rotate(45deg);
    }

    #navbar .element *:hover {
        color: #cccccc;
    }

    body {
        background-color: #ffffff;
    }

    #title-name {
        font-size: xxx-large;
        text-align: center;
    }

    #title-name {
        color: #0000ff;
        background-color: #00ddff;
        border: 5px solid #000000;
        margin-right: 10px;
        margin-left: 10px;
        margin-top: 20px;
    }

    #sidenav {
        z-index: 1;
        transition: 0.5s;
        background-color: #222222;
        opacity: 80%;
        height: 100%;
        position: fixed;
    }
</style>

<script>
    function toggleSideNav() {
        document.getElementById("sidenav").style.width = (document.getElementById("sidenav").style.width === "0px")?"250px":"0px";
        document.getElementById("sidenav-button").classList.toggle("change");
    }
</script>

<div id="navbar">
    <div id="nav" class="element">
        <div id="sidenav-button" onclick="toggleSideNav();">
            <div id="bar1"></div>
            <div id="bar2"></div>
            <div id="bar3"></div>
        </div>
        <span id="home-nav" onclick="window.location.href='/home/'">Home</span>
        <span id="forum-nav" onclick="window.location.href='/forum/'">Forum</span>
        <span id="game-nav" onclick="window.location.href='/webgame/'">Game</span>
    </div>
    <div id="account" class="element">
        <span id="account-nav" onclick="window.location.href='/login/'">Login</span>
    </div>
</div>

<div id="sidenav" style="width: 0px;">

</div>

<div id="title-name">Galleta Games and Software</div>
</html>